# ── Core dependencies ─────────────────────────────────────────────────────────
transformers>=4.36.0
datasets>=2.16.0
torchaudio>=2.0.0
scikit-learn>=1.3.0
numpy>=1.24.0
librosa>=0.10.0
umap-learn>=0.5.5
matplotlib>=3.7.0
seaborn>=0.13.0
torchcodec>=0.1.0
soundfile>=0.12.1
tqdm>=4.66.0
unicodedata2>=15.0.0

# ── PyTorch: install separately based on your platform ────────────────────────
#
# Mac (Apple Silicon M1/M2/M3/M4) — MPS acceleration built in:
#   pip install torch>=2.0.0
#
# Windows/Linux with NVIDIA GPU — CUDA:
#   pip install torch>=2.0.0 --index-url https://download.pytorch.org/whl/cu121
#
# Windows/Linux CPU only:
#   pip install torch>=2.0.0
#
# Run setup.py to handle this automatically.